import type { Metadata } from "next";
import { Geist } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "TRINITY DTV | 5-Year Destination Thailand Visa",
  description:
    "TRINITY DTV specialises in securing 5-Year Destination Thailand Visas (DTV) for the Soft Skills Muay Thai programme. Smooth, clear, and stress-free visa applications.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${geistSans.variable} font-sans antialiased`}>
        {children}
      </body>
    </html>
  );
}
